# Copiar a .env y ajustar según el entorno local.
# URL base del backend REST (backend/src/server.ts), usada por
# frontend/src/api/notesApi.ts. Vite solo expone al código de cliente las
# variables de entorno prefijadas con VITE_.

VITE_API_BASE_URL=http://localhost:3000
